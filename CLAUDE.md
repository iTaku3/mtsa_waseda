# MTSA Waseda - Claude Code 開発ガイド

このリポジトリは、MTSAを用いた研究開発用のリポジトリである。

Claude Codeがコードの解析・実装・修正を行う場合は、このドキュメントに記載されたルールを必ず守ること。

## 1. 基準ブランチ

Stepwise Controller Synthesis（SCS）に関する現在の研究では、以下のブランチを基準とする。

```text
SCS/DynamicPolicy-ApplyMinimize
```

原則として、このブランチ上で直接コードを書き換えない。

変更を行う場合は、このブランチから目的ごとの作業ブランチを作成する。

例：

```text
SCS/DynamicPolicy-ApplyMinimize
    ├── docs/claude-research-guidance
    ├── research/dirty-signature-minimiser
    ├── perf/minimiser-benchmark
    └── fix/minimiser-xxx
```

基本方針は、

```text
1つのブランチ = 1つの目的
```

とする。

変更完了後は、基準ブランチへのPull Requestを作成してマージする。

---

## 2. 最初に読むドキュメント

コードを変更する前に、タスクに応じて以下を読むこと。

リポジトリ構造：

```text
docs/PROJECT_STRUCTURE.md
```

Git・開発ルール：

```text
docs/DEVELOPMENT_WORKFLOW.md
```

LTS最小化関連：

```text
docs/research/MINIMISATION.md
```

特に最小化アルゴリズムを変更する場合は、

```text
docs/research/MINIMISATION.md
```

を必ず先に読むこと。

---

## 3. 実装前の基本ルール

コードを変更する前に、必ず既存コードを調査すること。

最低限、以下を確認する。

- 修正対象クラス
- そのクラスを呼び出している箇所
- 入力・出力となるデータ構造
- 関連する既存テスト
- 修正によって影響を受ける可能性がある処理
- 既存処理が保証している意味・振る舞い

コードを十分に理解せず、推測だけで既存実装を書き換えないこと。

---

## 4. 変更範囲

依頼された目的と関係のない変更を行わないこと。

特に、研究アルゴリズムの実装と同時に以下を行わない。

- Javaバージョンの変更
- Maven設定の変更
- ライブラリのアップデート
- 大規模なリファクタリング
- パッケージ構造の変更
- コード全体のフォーマット変更
- 関係のない警告の修正
- UIの整理
- 未使用コードの大量削除

必要な場合は別ブランチ・別Pull Requestで実施する。

---

## 5. Git操作

Claude Codeは、ユーザーから明示的な指示がない限り以下を実行しないこと。

```text
git push
git merge
git reset --hard
force push
shared branchのhistory rewrite
既存ブランチの削除
```

特に破壊的なGit操作は行わない。

まずユーザーが変更差分を確認できる状態まで進めること。

---

## 6. 最小化アルゴリズムに関する重要ルール

現在のLTS最小化実装は、

```text
maven-root/mtsa/src/main/java/ltsa/lts/Minimiser.java
```

に存在する。

新しい最小化アルゴリズムを実装するときは、最初から既存の `Minimiser` を削除・置換しない。

まず、

```text
既存アルゴリズム
```

と

```text
新しいアルゴリズム
```

を両方利用できる状態にする。

目的は、同一入力に対して両方を実行し、

- 正しさ
- 状態数
- 遷移数
- 実行時間
- メモリ使用量

を比較できるようにすることである。

---

## 7. 最小化で維持する必要がある処理

現在の `Minimiser#minimise()` は、状態の等価性判定だけではなく、その前後に複数の処理を行っている。

例：

- `removeNonDetTau()`
- tau遷移に関する処理
- tau closure
- END状態の特殊処理
- quotient LTSの生成
- reflexive tauの除去
- `MarkedCompactState` の状態番号更新

したがって、新しいアルゴリズムを実装するときに、

```text
Minimiser#minimise() 全体
```

を単純に別アルゴリズムへ置き換えてはいけない。

まずは、

```text
既存の前処理
    ↓
新しい等価状態計算
    ↓
既存と互換性のあるLTS構築
```

という構造を優先して検討すること。

詳細は、

```text
docs/research/MINIMISATION.md
```

を参照すること。

---

## 8. 正しさを優先する

研究目的は高速化であるが、

```text
高速になったが制御器の振る舞いが変わった
```

という実装は受け入れない。

少なくとも、既存最小化が維持している意味を新しい最小化でも維持する必要がある。

特に、

```text
制御器が実行可能なアクション列
```

が、最小化アルゴリズムを変更したことによって変化してはいけない。

ただし、最小化後の状態番号や同値クラスに付与される番号は一致する必要はない。

---

## 9. テスト

新しいアルゴリズムを実装した場合、既存テストを実行するだけではなく、新しいテストも追加すること。

特に、

```text
Legacy Minimiser
        vs
New Minimiser
```

という比較テストを用意する。

単純にJavaオブジェクトが一致するかではなく、LTSの意味が一致するかを確認すること。

---

## 10. ビルド

メインのMavenプロジェクトは、

```text
maven-root/mtsa
```

である。

基本的なビルドは、

```bash
cd maven-root/mtsa
mvn clean
mvn install -DskipTests=true
```

テストを実行する場合は、

```bash
mvn test
```

を使用する。

このリポジトリは古いJava/Maven設定を含んでいる。

ビルドエラーが発生した場合も、最初からJavaやMavenの設定を変更せず、まず現在の環境と既存設定を確認すること。

---

## 11. 最小化実装を開始する前に報告する内容

最小化アルゴリズムを変更するタスクでは、コードを書き始める前に以下を整理して報告すること。

- 現在の `Minimiser` のアルゴリズム
- `Minimiser` の呼び出し経路
- tau処理
- END状態の扱い
- ERROR状態の扱い
- 非決定的遷移の表現
- `CompactState`
- `EventState`
- quotient LTSの生成方法
- `MarkedCompactState` の扱い
- 利用できる既存テスト
- 新しく追加する必要があるテスト
- 新実装で変更するファイル
- 新実装で変更しないファイル

その後、最小限の変更で実装方針を提案すること。

---

## 12. 実装完了の条件

「コンパイルが通った」だけでは完了としない。

少なくとも以下を満たすこと。

- 既存最小化を利用できる
- 新しい最小化を利用できる
- 両者を比較できる
- 関連テストが通る
- 新しい最小化用テストが存在する
- 特殊なLTS構造について検証されている
- 状態数を比較できる
- 遷移数を比較できる
- 最小化単体の実行時間を比較できる
- 必要に応じて新アルゴリズム特有の処理回数も計測できる
